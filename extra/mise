function __setup_mise {
	export MISE_DATA_DIR=${HOME}/.local/share/mise
	export MISE_CACHE_DIR=${HOME}/.cache/mise
	export MISE_CONFIRM=yes
	export MISE_EXPERIMENTAL=1
}

if command -v mise &>/dev/null; then
	__setup_mise
fi
